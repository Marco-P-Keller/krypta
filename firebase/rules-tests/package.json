{
  "name": "krypta-rules-tests",
  "private": true,
  "description": "Prüft firebase/firestore.rules im Emulator (Sealed Sender). Braucht Java.",
  "scripts": {
    "test": "cd ../.. && firebase emulators:exec --only firestore --project demo-krypta \"$(which node) firebase/rules-tests/sealed_sender.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0"
  }
}
