{
  "name": "krypta-ecc-functions",
  "description": "Krypta ECC Cloud Functions - message relay, push notifications, cleanup",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "check": "node --check index.js && node -e \"require('./index.js'); console.log('module loads, exports:', Object.keys(require('./index.js')).join(', '))\""
  },
  "engines": {
    "node": "22"
  },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^6.6.0"
  },
  "private": true
}
